<%= form_for [item, item.quizzes.build], :remote => true, :html => { :class => 'form-inline' } do |f| %>
  <%= f.text_area :content, :value => item.content, :rows => 4, :class => 'quiz-text' %><br />
  <%= f.submit nil, :class => 'btn btn-primary btn-small' %>
<% end %>
